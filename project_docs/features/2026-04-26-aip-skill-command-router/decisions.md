# Decisions

- 2026-04-26: Implement `$aip` behavior through explicit skill routing rules plus a Python `aip.py` command router.
- 2026-04-26: Support `init`, `start`, `resume`, and `check` first; defer richer automation.
- 2026-04-26: Keep aliases in the skill layer, not in the CLI, so terminal behavior remains small and predictable.
